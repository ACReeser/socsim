import { TraitJob } from "../World";
import { IBean } from "./Agent";
import { ICity } from "./City";

export function CityBeanTrySetJob(city: ICity, bean: IBean, job: TraitJob): boolean{
    //TODO REDUX TODO
    // if (this.city?.tryGetJob(this, job)){
    //     this.city.unsetJob(this);
    //     bean.job = job;
    //     return true;
    // }
    return false;
}